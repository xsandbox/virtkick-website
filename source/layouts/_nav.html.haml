= navbar fixed: :top, container: 'fluid' do
  = navbar_header brand: '<img src="/img/logo/virtkick-logo-pixelart-32.png" class="pix" /> virtkick', brand_link: '/'
  = navbar_collapse do
    = navbar_group class: 'navbar-right', id: 'menu' do
      = partial 'layouts/nav_items', locals: {header: true}
